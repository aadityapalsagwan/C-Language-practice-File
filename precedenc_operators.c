#include<stdio.h>
 int main(){
 	int x=2;
 	int y=3;
 	printf("the value of 9*x - 7*y is %d \n",9*x -7*y);
 	printf("the value of 9*x / 2*y %d\n ",9*x / 2*y); // (*,/) is left to right associtavity
 	return 0;
 }
